pip install pyltspice
pip install ltspice
pip install mplcursors
put .py and .asc files in the same folder

In Spyder, so that plots show up as a separate window:
tools->preferences->IPython console->Backend: automatic

execute python script
